# Dependencies

This application uses AndroidX (Apache-2.0), OkHttp/Okio (Apache-2.0), and ZXing Android Embedded / ZXing (Apache-2.0). The Gradle wrapper is Apache-2.0. Versions are pinned in `app/build.gradle.kts` and the wrapper properties. Their original copyright and license notices apply.

The Web client is supplied by the user's connected DeepSeek Harness Host and plugins; those components retain their own licenses. This APK does not bundle the Harness client or model credentials.
